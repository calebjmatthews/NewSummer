export default class Trait {
  constructor(name, loci, completeDominance, statNames, statModifiers) {
    this.name = name;
    this.loci = loci;
    this.completeDominance = completeDominance;
    this.statNames = statNames;
    this.statModifiers = statModifiers;
  }
}
